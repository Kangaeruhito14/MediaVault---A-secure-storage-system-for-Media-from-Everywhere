---
import PublicLayout from '../layouts/PublicLayout.astro';

// Static CTAs — auth happens client-side in the /app area (E2EE).
const primaryCtaText = 'Get Started';
const primaryCtaPath = '/app';
const secondaryCtaText = 'How it works';
const secondaryCtaPath = '/about';

const origin = import.meta.env.PUBLIC_SITE_URL?.replace(/\/$/, '') || Astro.url.origin;

// Alternate names people actually search for — surfaced as schema.org alternateName
// so the same product can match many "media vault / store / locker / archive" queries.
const aliases = [
  'Media Reservoir', 'Media Vault', 'MediaVault', 'Open Media Vault',
  'Media Store', 'Media Storage', 'Media Repository', 'Media Archive',
  'Media Library', 'Media Locker', 'Media Safe', 'Media Reserve',
  'Media Cloud', 'Media Backup', 'Media Hub', 'Media Depot', 'Media Trove', 'Media Haven',
  'Asset Vault', 'Asset Reserve', 'Asset Haven', 'Asset Locker', 'Digital Asset Vault',
  'Photo Vault', 'Video Vault', 'Encrypted Media Vault', 'Encrypted Photo Vault',
  'Secure Media Storage', 'Private Media Library', 'Zero-Knowledge Vault',
  'Encrypted Cloud Storage', 'Private Cloud Storage',
];
const jsonLd = [
  {
    '@context': 'https://schema.org',
    '@type': 'WebSite',
    name: 'Media Reservoir',
    alternateName: aliases,
    url: origin,
    description:
      'Free, open-source, end-to-end encrypted vault — a private Google Photos & cloud-drive alternative that encrypts photos, videos, audio, PDFs, and documents in your browser and stores them in cloud storage you own.',
  },
  {
    '@context': 'https://schema.org',
    '@type': 'SoftwareApplication',
    name: 'Media Reservoir',
    alternateName: aliases,
    applicationCategory: 'MultimediaApplication',
    operatingSystem: 'Any modern web browser',
    offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
    description:
      'End-to-end encrypted vault for photos, videos, audio, PDFs, and documents. Files are encrypted in the browser and stored in your own S3-compatible cloud; the operator can never read them. Includes seekable encrypted streaming, in-app document/PDF viewing, and recovery-key reset.',
    featureList: [
      'End-to-end encryption — keys never leave your device',
      'Bring your own storage (Cloudflare R2, Backblaze B2, Wasabi, S3)',
      'Zero-knowledge: the operator stores only ciphertext',
      'Seekable encrypted video & audio streaming',
      'Encrypted thumbnails for fast galleries',
      'Recovery-key password reset; multi-device; installable PWA',
    ],
    url: origin,
  },
  {
    '@context': 'https://schema.org',
    '@type': 'Organization',
    name: 'Media Reservoir',
    alternateName: aliases,
    url: origin,
    logo: origin + '/og-image.png',
    description:
      'Free, open-source, end-to-end encrypted media vault — a private alternative to Google Photos that stores your files in storage you own.',
    sameAs: [
      'https://github.com/Kangaeruhito14/MediaVault---A-secure-storage-system-for-Media-from-Everywhere',
    ],
  },
];
---

<PublicLayout
  title="Media Reservoir — End-to-End Encrypted Media, on Storage You Own"
  description="Media Reservoir encrypts your photos, videos, audio, PDFs, and documents in your browser, then stores them in your own cloud (Cloudflare R2, Backblaze B2, Wasabi, S3). The keys never leave your device — so not even we can read your files. Free, open-source, multi-device."
  jsonLd={jsonLd}
>

  <!-- ══ HERO ══ -->
  <section class="hero">
    <div class="section-container hero-inner">
      <div class="eyebrow" data-reveal>
        <img src="/logo.png" alt="" width="18" height="18" />
        <span>End-to-end encrypted · Zero-knowledge · Your storage</span>
      </div>

      <h1 class="hero-title" data-reveal data-reveal-delay="60">
        Your media, locked before<br class="hero-br" /> it leaves your device.
      </h1>

      <p class="hero-lead" data-reveal data-reveal-delay="120">
        Media Reservoir seals your photos, videos, audio and documents in your browser —
        then hands the encrypted files to a cloud bucket <strong>you own</strong>. The keys
        never touch our servers, so no one else can open them. Not advertisers. Not hackers.
        <strong>Not even us.</strong>
      </p>

      <div class="cta-row" data-reveal data-reveal-delay="180">
        <a href={primaryCtaPath} class="btn-hero-primary">
          {primaryCtaText}
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <line x1="5" y1="12" x2="19" y2="12"></line>
            <polyline points="12 5 19 12 12 19"></polyline>
          </svg>
        </a>
        <a href={secondaryCtaPath} class="btn-hero-ghost">{secondaryCtaText}</a>
      </div>

      <ul class="trust-row" data-reveal data-reveal-delay="220">
        <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>AES-256 in your browser</li>
        <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>Bring your own storage</li>
        <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>Free &amp; open-source</li>
      </ul>

      <figure class="hero-visual" data-reveal data-reveal-delay="280">
        <div
          class="hero-img"
          role="img"
          aria-label="A glowing encrypted vault holding photos, video, and audio — sealed with a lock and key, floating in cloud storage you own."
        ></div>
      </figure>
    </div>
  </section>

  <!-- ══ STATS BAND ══ -->
  <section class="stats">
    <div class="section-container stats-grid">
      <div class="stat" data-reveal>
        <div class="stat-num">0</div>
        <p class="stat-label">Files we can read. Your keys never leave your device.</p>
      </div>
      <div class="stat" data-reveal data-reveal-delay="80">
        <div class="stat-num">100%</div>
        <p class="stat-label">End-to-end encrypted — before anything is uploaded.</p>
      </div>
      <div class="stat" data-reveal data-reveal-delay="160">
        <div class="stat-num">&infin;</div>
        <p class="stat-label">Bring your own storage — any S3-compatible cloud.</p>
      </div>
    </div>
  </section>

  <!-- ══ FEATURES (icons from illustrations.png) ══ -->
  <section class="features">
    <div class="section-container">
      <header class="section-head" data-reveal>
        <span class="eyebrow-mono">How it protects you</span>
        <h2 class="section-title">Private by architecture, not by promise.</h2>
        <p class="section-lead">Security you don't have to take on faith. The design itself makes your files
          unreadable to us — there is nothing to trust, only math to verify.</p>
      </header>

      <div class="features-grid">
        <article class="feature" data-reveal>
          <span class="fi fi-shield" aria-hidden="true"></span>
          <h3>Zero-knowledge encryption</h3>
          <p>Every file is sealed in your browser with AES-256 before it's uploaded. We only ever hold ciphertext we cannot read.</p>
        </article>
        <article class="feature" data-reveal data-reveal-delay="60">
          <span class="fi fi-cloud" aria-hidden="true"></span>
          <h3>Bring your own storage</h3>
          <p>Connect Cloudflare R2, Backblaze B2, Wasabi, Dropbox, or any S3 bucket. Files live in your account — never on our servers.</p>
        </article>
        <article class="feature" data-reveal data-reveal-delay="120">
          <span class="fi fi-stream" aria-hidden="true"></span>
          <h3>Seekable streaming</h3>
          <p>Range-based decryption reads only the slice you're watching, so you can jump anywhere in a multi-GB video instantly.</p>
        </article>
        <article class="feature" data-reveal>
          <span class="fi fi-photo" aria-hidden="true"></span>
          <h3>Encrypted thumbnails</h3>
          <p>Previews are generated and encrypted in your browser, so galleries load fast without ever exposing the full files.</p>
        </article>
        <article class="feature" data-reveal data-reveal-delay="60">
          <span class="fi fi-key" aria-hidden="true"></span>
          <h3>Recovery key</h3>
          <p>Forgot your password? A one-time recovery key restores access. Lose both and not even we can help — that's the whole point.</p>
        </article>
        <article class="feature" data-reveal data-reveal-delay="120">
          <span class="fi fi-devices" aria-hidden="true"></span>
          <h3>Every device</h3>
          <p>Sign in from any device; your library syncs through a tiny encrypted index. Installable as an app (PWA) on phone and desktop.</p>
        </article>
      </div>
    </div>
  </section>

  <!-- ══ ENCRYPTION FLOW ══ -->
  <section class="flow">
    <div class="section-container">
      <header class="section-head" data-reveal>
        <span class="eyebrow-mono">Zero-knowledge by design</span>
        <h2 class="section-title">Sealed before we ever touch it.</h2>
        <p class="section-lead">Encryption happens on your device. Everything downstream — the network, our
          servers, your cloud — only ever handles ciphertext. There is nothing for us to read, leak, or hand over.</p>
      </header>

      <div class="flow-diagram" data-reveal data-reveal-delay="80">
        <div class="flow-node">
          <div class="flow-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="14" rx="2"/><circle cx="8.5" cy="9" r="1.4"/><path d="m4 15 4.5-4 3.5 3 3-2.5 4 3.5"/></svg>
          </div>
          <div class="flow-label">Your device</div>
          <div class="flow-sub">The original file</div>
        </div>

        <div class="flow-link"><span class="flow-dot"></span><span class="flow-dot"></span><span class="flow-dot"></span></div>

        <div class="flow-node flow-node-hl">
          <div class="flow-icon flow-lock">
            <span class="flow-pulse"></span>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><rect x="4.5" y="11" width="15" height="9.5" rx="2.2"/><path d="M8 11V7.5a4 4 0 0 1 8 0V11"/><circle cx="12" cy="15.5" r="1.4"/></svg>
          </div>
          <div class="flow-label">Encrypted in your browser</div>
          <div class="flow-sub">AES-256 · your key never leaves</div>
        </div>

        <div class="flow-link flow-link-cipher"><span class="flow-dot"></span><span class="flow-dot"></span><span class="flow-dot"></span></div>

        <div class="flow-node">
          <div class="flow-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M17.5 19a4.5 4.5 0 0 0 .8-8.93A6 6 0 0 0 6.6 9.2 4 4 0 0 0 6.5 19z"/></svg>
          </div>
          <div class="flow-label">Your cloud bucket</div>
          <div class="flow-sub">Ciphertext, in storage you own</div>
        </div>
      </div>

      <div class="flow-callout" data-reveal data-reveal-delay="160">
        <span class="flow-callout-dot"></span>
        <span class="flow-callout-label">All our servers ever see:</span>
        <code class="flow-cipher" id="flow-cipher">a3f9c1e08b2d47…</code>
      </div>
    </div>
  </section>

  <!-- ══ HOW IT WORKS ══ -->
  <section class="workflow">
    <div class="section-container">
      <header class="section-head" data-reveal>
        <span class="eyebrow-mono">Quick start</span>
        <h2 class="section-title">Three steps to a private library.</h2>
      </header>

      <div class="workflow-steps">
        <div class="workflow-step" data-reveal>
          <div class="step-badge">1</div>
          <h3>Create your account</h3>
          <p>Sign up — your password derives your encryption key right in the browser. Save your one-time recovery key.</p>
        </div>
        <div class="workflow-step" data-reveal data-reveal-delay="80">
          <div class="step-badge">2</div>
          <h3>Connect your storage</h3>
          <p>Link your own cloud bucket (R2, Backblaze B2, Wasabi, S3). Files go straight there — never through us.</p>
        </div>
        <div class="workflow-step" data-reveal data-reveal-delay="160">
          <div class="step-badge">3</div>
          <h3>Upload &amp; enjoy</h3>
          <p>Drag in photos and videos — encrypted locally, then streamed, searched, and viewed across all your devices.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ══ SHOWCASE / COMMERCIAL BAND (image left, copy right) ══ -->
  <section class="showcase">
    <div class="section-container showcase-inner">
      <div class="showcase-visual" data-reveal role="img" aria-label="The Media Reservoir vault holding encrypted photos, videos, and audio inside storage you own."></div>
      <div class="showcase-copy" data-reveal data-reveal-delay="80">
        <span class="eyebrow-mono">One vault, everything</span>
        <h2 class="section-title showcase-title">Everything you own, sealed in one place.</h2>
        <p class="showcase-lead">
          Photos, 4K video, voice memos, PDFs, and documents — encrypted in your browser and locked
          inside storage you control. Browse, stream, and search your whole library from any device,
          while the vault stays sealed to everyone but you.
        </p>
        <ul class="showcase-points">
          <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>Encrypted before it ever leaves your device</li>
          <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>Stored in a cloud bucket you own</li>
          <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>Streamed and searched from every device</li>
        </ul>
        <a href={secondaryCtaPath} class="btn-hero-primary">
          See how it works
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <line x1="5" y1="12" x2="19" y2="12"></line>
            <polyline points="12 5 19 12 12 19"></polyline>
          </svg>
        </a>
      </div>
    </div>
  </section>

  <!-- ══ "CALL IT WHAT YOU LIKE" (readable synonym coverage for search) ══ -->
  <section class="aka">
    <div class="section-container aka-inner" data-reveal>
      <span class="eyebrow-mono">Call it what you like</span>
      <h2 class="aka-title">One vault. Every name you searched for.</h2>
      <div class="aka-cloud" id="aka-cloud" role="group" aria-label="Names people call Media Reservoir">
        <button type="button" class="aka-chip is-accent" aria-pressed="true">Encrypted photo vault</button>
        <button type="button" class="aka-chip" aria-pressed="false">Private media library</button>
        <button type="button" class="aka-chip" aria-pressed="false">Zero-knowledge asset locker</button>
        <button type="button" class="aka-chip" aria-pressed="false">Secure media store</button>
        <button type="button" class="aka-chip" aria-pressed="false">Media Vault</button>
        <button type="button" class="aka-chip" aria-pressed="false">Photo Locker</button>
        <button type="button" class="aka-chip" aria-pressed="false">Media Archive</button>
        <button type="button" class="aka-chip" aria-pressed="false">Google Photos alternative</button>
        <button type="button" class="aka-chip" aria-pressed="false">Encrypted cloud storage</button>
        <button type="button" class="aka-chip" aria-pressed="false">Digital asset vault</button>
        <button type="button" class="aka-chip" aria-pressed="false">Private cloud storage</button>
        <button type="button" class="aka-chip" aria-pressed="false">Media Trove</button>
      </div>
      <p class="aka-foot">However you found it — that's <strong>Media Reservoir</strong>.</p>
    </div>
  </section>

  <!-- ══ CTA BANNER (aurora backdrop) ══ -->
  <section class="cta-banner">
    <div class="section-container cta-banner-inner" data-reveal>
      <h2 class="cta-banner-title">Take back your memories.</h2>
      <p class="cta-banner-desc">Free, open-source, end-to-end encrypted. Your media, your cloud, your keys — not the tech giants'.</p>
      <a href={primaryCtaPath} class="btn-hero-primary btn-cta">{primaryCtaText}
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <line x1="5" y1="12" x2="19" y2="12"></line>
          <polyline points="12 5 19 12 12 19"></polyline>
        </svg>
      </a>
    </div>
  </section>

</PublicLayout>

<script>
  // Live "ciphertext" scramble in the encryption-flow callout — cycles hex while
  // in view for a sense of active encryption. Paused off-screen + for reduced-motion.
  function initCipherScramble() {
    const el = document.getElementById('flow-cipher');
    if (!el) return;
    (window as any).__cipherCleanup?.();
    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;
    const hex = '0123456789abcdef';
    let timer = 0;
    const tick = () => {
      let s = '';
      for (let i = 0; i < 12; i++) s += hex[(Math.random() * 16) | 0];
      el.textContent = s + '…';
    };
    const io = new IntersectionObserver((entries) => {
      if (entries[0].isIntersecting && !timer) timer = window.setInterval(tick, 90);
      else if (!entries[0].isIntersecting && timer) { clearInterval(timer); timer = 0; }
    }, { threshold: 0.25 });
    io.observe(el);
    (window as any).__cipherCleanup = () => { if (timer) clearInterval(timer); io.disconnect(); };
  }

  // "Call it what you like" chips — a playful single-select (no real output);
  // clicking a name highlights it and clears the others.
  function initAkaChips() {
    const cloud = document.getElementById('aka-cloud');
    if (!cloud) return;
    cloud.addEventListener('click', (e) => {
      const chip = (e.target as HTMLElement).closest('.aka-chip');
      if (!chip) return;
      cloud.querySelectorAll('.aka-chip').forEach((c) => {
        const on = c === chip;
        c.classList.toggle('is-accent', on);
        c.setAttribute('aria-pressed', String(on));
      });
    });
  }

  function initHome() { initCipherScramble(); initAkaChips(); }
  document.addEventListener('astro:page-load', initHome);
</script>

<style>
  .section-container {
    max-width: 1160px;
    margin: 0 auto;
    padding: 0 24px;
    position: relative;
    z-index: 2;
  }

  /* Shared section headers */
  .section-head { text-align: center; max-width: 680px; margin: 0 auto 60px; }
  .eyebrow-mono {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px; font-weight: 500; text-transform: uppercase;
    color: var(--accent); letter-spacing: 0.14em; display: block; margin-bottom: 14px;
  }
  .section-title {
    font-size: clamp(27px, 3.6vw, 38px); font-weight: 600;
    color: var(--text-primary); letter-spacing: -0.035em; line-height: 1.12; margin: 0;
  }
  .section-lead {
    font-size: 16px; line-height: 1.65; color: var(--text-body); margin: 18px auto 0; max-width: 600px;
  }

  /* ══ HERO ══ */
  .hero {
    position: relative;
    padding: 128px 0 96px;
    text-align: center;
    overflow: hidden;
    /* Light (default): the pastel aurora (owner's ornament-light.png) fades into
       the page, with a light scrim so dark text stays legible. */
    background:
      linear-gradient(180deg, rgba(247,248,252,0.42) 0%, rgba(247,248,252,0.12) 34%, rgba(247,248,252,0.86) 80%, var(--bg) 100%),
      url('/ornament-light.png') center top / cover no-repeat;
  }
  /* Dark: the aurora ornament backdrop (owner's ornament.png) fades into the page,
     with a dark scrim so text stays legible. */
  :global(html[data-theme='dark']) .hero {
    background:
      linear-gradient(180deg, rgba(9,10,15,0.60) 0%, rgba(9,10,15,0.34) 34%, rgba(9,10,15,0.86) 78%, var(--bg) 100%),
      radial-gradient(ellipse 90% 60% at 50% 0%, rgba(99,102,241,0.20), transparent 70%),
      url('/ornament.png') center top / cover no-repeat;
  }

  .hero-inner { display: flex; flex-direction: column; align-items: center; }

  .eyebrow {
    display: inline-flex; align-items: center; gap: 9px;
    padding: 7px 16px 7px 10px; margin-bottom: 26px;
    border: 1px solid var(--border-med); border-radius: 100px;
    background: var(--surface); backdrop-filter: blur(8px);
    box-shadow: var(--shadow-sm);
    font-size: 12.5px; font-weight: 500; color: var(--text-body); letter-spacing: 0.01em;
  }
  :global(html[data-theme='dark']) .eyebrow { background: rgba(255,255,255,0.04); box-shadow: none; }
  .eyebrow img { display: block; filter: drop-shadow(0 1px 4px rgba(124,58,237,0.5)); }

  .hero-title {
    font-size: clamp(38px, 6.2vw, 66px); font-weight: 600;
    color: var(--text-primary); letter-spacing: -0.045em; line-height: 1.04;
    max-width: 860px; margin: 0 0 24px;
  }

  .hero-lead {
    font-size: clamp(16px, 2.4vw, 19px); color: var(--text-body);
    line-height: 1.62; max-width: 640px; margin: 0 0 36px;
  }
  .hero-lead strong { color: var(--text-primary); font-weight: 600; }

  .cta-row { display: flex; gap: 14px; align-items: center; justify-content: center; }
  @media (max-width: 480px) { .cta-row { flex-direction: column; width: 100%; } }

  .btn-hero-primary {
    display: inline-flex; align-items: center; justify-content: center; gap: 8px;
    height: 50px; padding: 0 30px; border-radius: 100px;
    font-size: 15px; font-weight: 600; letter-spacing: -0.01em;
    background: linear-gradient(135deg, #6366f1, #7c3aed);
    color: #fff;
    box-shadow: 0 10px 30px -8px rgba(109, 74, 232, 0.6), inset 0 1px 0 rgba(255,255,255,0.18);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }
  .btn-hero-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 16px 40px -10px rgba(109, 74, 232, 0.75), inset 0 1px 0 rgba(255,255,255,0.22);
    color: #fff;
  }
  .btn-hero-primary:active { transform: translateY(0); }

  .btn-hero-ghost {
    display: inline-flex; align-items: center; justify-content: center;
    height: 50px; padding: 0 28px; border-radius: 100px;
    font-size: 15px; font-weight: 500;
    background: var(--surface); color: var(--text-primary);
    border: 1px solid var(--border-med); box-shadow: var(--shadow-sm);
    transition: border-color 0.2s ease, background 0.2s ease, transform 0.2s ease;
  }
  .btn-hero-ghost:hover {
    border-color: var(--border-strong); background: var(--bg-inset);
    transform: translateY(-1px); color: var(--text-primary);
  }
  :global(html[data-theme='dark']) .btn-hero-ghost { background: rgba(255,255,255,0.05); box-shadow: none; }
  :global(html[data-theme='dark']) .btn-hero-ghost:hover { background: rgba(255,255,255,0.08); }
  @media (max-width: 480px) { .btn-hero-primary, .btn-hero-ghost { width: 100%; } }

  .trust-row {
    list-style: none; margin: 30px 0 0; padding: 0;
    display: flex; flex-wrap: wrap; gap: 10px 26px; justify-content: center;
  }
  .trust-row li {
    display: inline-flex; align-items: center; gap: 8px;
    font-size: 13.5px; color: var(--text-body);
  }
  .trust-row svg { width: 15px; height: 15px; color: #34d399; flex: none; }

  /* Hero centerpiece (owner's glass-vault) — swapped per theme: the light-bg
     render in light mode, the cosmic dark-bg render in dark mode. Edge-masked
     both ways so its background dissolves seamlessly into the section. */
  .hero-visual { position: relative; width: 100%; max-width: 800px; margin: 60px auto 0; }
  .hero-visual::before {
    content: ''; position: absolute; inset: 6% 8% 10%;
    background: radial-gradient(ellipse at 50% 48%, rgba(96,132,255,0.32), rgba(124,58,237,0.15) 46%, transparent 72%);
    filter: blur(80px); z-index: 0; pointer-events: none;
  }
  :global(html[data-theme='dark']) .hero-visual::before {
    background: radial-gradient(ellipse at 50% 48%, rgba(96,132,255,0.42), rgba(124,58,237,0.20) 46%, transparent 72%);
  }
  .hero-img {
    position: relative; z-index: 1; width: 100%; aspect-ratio: 3 / 2; display: block;
    background: url('/hero-light.png') center / contain no-repeat;
    -webkit-mask: radial-gradient(ellipse 68% 68% at 50% 47%, #000 34%, transparent 78%);
            mask: radial-gradient(ellipse 68% 68% at 50% 47%, #000 34%, transparent 78%);
    animation: heroFloat 8s ease-in-out infinite;
  }
  :global(html[data-theme='dark']) .hero-img { background-image: url('/hero.png'); }
  @keyframes heroFloat { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-14px); } }
  @media (prefers-reduced-motion: reduce) { .hero-img { animation: none; } }

  /* ══ STATS ══ */
  .stats {
    border-top: 1px solid var(--border); border-bottom: 1px solid var(--border);
    background: var(--bg-inset); padding: 46px 0;
  }
  .stats-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 40px; text-align: center; }
  @media (max-width: 768px) { .stats-grid { grid-template-columns: 1fr; gap: 30px; } }
  .stat { display: flex; flex-direction: column; align-items: center; gap: 10px; }
  .stat-num {
    font-size: clamp(36px, 5vw, 52px); font-weight: 700; letter-spacing: -0.04em; line-height: 1;
    background: linear-gradient(135deg, var(--text-primary) 20%, #7c6cff);
    -webkit-background-clip: text; background-clip: text;
    -webkit-text-fill-color: transparent; color: transparent;
  }
  .stat-label { font-size: 13px; color: var(--text-body); max-width: 240px; margin: 0; }

  /* ══ FEATURES ══ */
  .features { position: relative; padding: 100px 0; }
  /* A faint indigo wash keeps the dark section from reading as flat black. */
  :global(html[data-theme='dark']) .features::before {
    content: ''; position: absolute; inset: 0; z-index: 0; pointer-events: none;
    background: radial-gradient(ellipse 62% 48% at 50% 0%, rgba(99,102,241,0.10), transparent 72%);
  }
  .features-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; }
  @media (max-width: 940px) { .features-grid { grid-template-columns: repeat(2, 1fr); } }
  @media (max-width: 600px) { .features-grid { grid-template-columns: 1fr; } }

  .feature {
    background: var(--surface);
    border: 1px solid var(--border); border-radius: 18px;
    padding: 30px 26px; box-shadow: var(--shadow-sm);
    transition: transform 0.3s cubic-bezier(0.16,1,0.3,1), border-color 0.3s ease, box-shadow 0.3s ease;
  }
  .feature:hover {
    transform: translateY(-4px); border-color: var(--border-strong);
    box-shadow: 0 20px 50px -24px rgba(99,102,241,0.5);
  }
  .feature h3 { font-size: 16px; font-weight: 600; color: var(--text-primary); margin: 0 0 8px; }
  .feature p { font-size: 13.5px; line-height: 1.55; color: var(--text-body); margin: 0; }

  /* Feature icons — the owner's transparent icon set, theme-swapped: the light
     variant (0X light) on a soft light tile, the dark/neon variant (0X dark) on a
     near-black tile. Transparent PNGs → shown as-is (no blend), centred, contained. */
  .fi {
    position: relative; display: block; width: 62px; height: 62px; margin-bottom: 18px;
    border-radius: 16px; overflow: hidden;
    background: linear-gradient(150deg, #f2f4fd 0%, #e6ebfa 100%);
    border: 1px solid rgba(99,102,241,0.16);
    box-shadow: inset 0 1px 0 rgba(255,255,255,0.7), 0 6px 16px -10px rgba(79,70,229,0.4);
  }
  :global(html[data-theme='dark']) .fi {
    background: linear-gradient(150deg, #1a1e2e 0%, #0f111b 100%);
    border-color: rgba(150,162,214,0.16);
    box-shadow: inset 0 1px 0 rgba(255,255,255,0.05), 0 8px 20px -8px rgba(30,26,80,0.55);
  }
  .fi::before {
    content: ''; position: absolute; inset: 6px;
    background-repeat: no-repeat; background-position: center; background-size: contain;
  }
  /* Light variant — default. */
  .fi-shield::before  { background-image: url('/icons/shield-light.png'); }
  .fi-cloud::before   { background-image: url('/icons/cloud-light.png'); }
  .fi-stream::before  { background-image: url('/icons/stream-light.png'); }
  .fi-photo::before   { background-image: url('/icons/photo-light.png'); }
  .fi-key::before     { background-image: url('/icons/key-light.png'); }
  .fi-devices::before { background-image: url('/icons/devices-light.png'); }
  /* Dark variant. */
  :global(html[data-theme='dark']) .fi-shield::before  { background-image: url('/icons/shield-dark.png'); }
  :global(html[data-theme='dark']) .fi-cloud::before   { background-image: url('/icons/cloud-dark.png'); }
  :global(html[data-theme='dark']) .fi-stream::before  { background-image: url('/icons/stream-dark.png'); }
  :global(html[data-theme='dark']) .fi-photo::before   { background-image: url('/icons/photo-dark.png'); }
  :global(html[data-theme='dark']) .fi-key::before     { background-image: url('/icons/key-dark.png'); }
  :global(html[data-theme='dark']) .fi-devices::before { background-image: url('/icons/devices-dark.png'); }

  /* ══ ENCRYPTION FLOW ══ */
  .flow { padding: 100px 0; background: var(--bg-inset); border-top: 1px solid var(--border); border-bottom: 1px solid var(--border); }
  .flow-diagram { display: flex; align-items: flex-start; justify-content: center; gap: 8px; margin: 0 auto; max-width: 900px; }
  .flow-node { display: flex; flex-direction: column; align-items: center; text-align: center; gap: 11px; width: 210px; flex: none; }
  .flow-icon {
    width: 74px; height: 74px; border-radius: 20px; position: relative;
    display: flex; align-items: center; justify-content: center;
    border: 1px solid var(--border-med); background: var(--surface); color: #7c8cff;
    box-shadow: var(--shadow-md);
  }
  .flow-icon svg { width: 32px; height: 32px; }
  .flow-node-hl .flow-icon.flow-lock {
    color: #fff; border: none;
    background: linear-gradient(140deg, #6366f1, #7c3aed);
    box-shadow: 0 14px 34px -10px rgba(109, 74, 232, 0.7);
  }
  .flow-pulse { position: absolute; inset: -5px; border-radius: 24px; border: 2px solid rgba(124,58,237,0.55); animation: flowPulse 2.4s ease-out infinite; }
  @keyframes flowPulse { 0% { transform: scale(0.92); opacity: 0.8; } 100% { transform: scale(1.32); opacity: 0; } }
  .flow-label { font-size: 14px; font-weight: 600; color: var(--text-primary); }
  .flow-sub { font-size: 12px; color: var(--text-muted); max-width: 170px; }

  .flow-link {
    flex: 1; min-width: 48px; height: 2px; position: relative; margin-top: 37px;
    background: linear-gradient(90deg, transparent, var(--border-strong) 20% 80%, transparent);
  }
  .flow-dot {
    position: absolute; top: 50%; width: 6px; height: 6px; border-radius: 50%;
    background: #6366f1; color: #6366f1; transform: translateY(-50%);
    box-shadow: 0 0 8px currentColor; animation: flowDot 2.2s linear infinite;
  }
  .flow-dot:nth-child(2) { animation-delay: 0.73s; }
  .flow-dot:nth-child(3) { animation-delay: 1.46s; }
  .flow-link-cipher .flow-dot { background: #7c3aed; color: #7c3aed; }
  @keyframes flowDot { 0% { left: 2%; opacity: 0; } 12% { opacity: 1; } 88% { opacity: 1; } 100% { left: 98%; opacity: 0; } }

  .flow-callout {
    display: flex; align-items: center; gap: 10px; width: max-content; max-width: 92%;
    margin: 48px auto 0; padding: 10px 18px;
    border: 1px solid var(--border-med); border-radius: 100px; background: var(--surface); box-shadow: var(--shadow-sm);
    font-size: 13px;
  }
  .flow-callout-dot { width: 7px; height: 7px; border-radius: 50%; background: #7c3aed; box-shadow: 0 0 9px #7c3aed; flex: none; }
  .flow-callout-label { color: var(--text-muted); }
  .flow-cipher { font-family: 'JetBrains Mono', monospace; color: #a78bfa; font-weight: 500; letter-spacing: 0.03em; }

  @media (max-width: 760px) {
    .flow-diagram { flex-direction: column; align-items: center; gap: 8px; }
    .flow-node { width: 100%; }
    .flow-link { width: 2px; height: 34px; min-width: 0; margin: 2px 0; background: linear-gradient(180deg, transparent, var(--border-strong), transparent); }
    .flow-dot { left: 50%; top: 0; transform: translateX(-50%); animation-name: flowDotV; }
    @keyframes flowDotV { 0% { top: 2%; opacity: 0; } 12% { opacity: 1; } 88% { opacity: 1; } 100% { top: 90%; opacity: 0; } }
  }
  @media (prefers-reduced-motion: reduce) { .flow-dot, .flow-pulse { animation: none; } .flow-dot { opacity: 0.9; } }

  /* ══ WORKFLOW ══ */
  .workflow { position: relative; padding: 100px 0; }
  :global(html[data-theme='dark']) .workflow::before {
    content: ''; position: absolute; inset: 0; z-index: 0; pointer-events: none;
    background: radial-gradient(ellipse 55% 55% at 50% 100%, rgba(124,58,237,0.10), transparent 72%);
  }
  .workflow-steps { display: grid; grid-template-columns: repeat(3, 1fr); gap: 32px; position: relative; }
  .workflow-steps::before {
    content: ''; position: absolute; top: 23px; left: 16.6%; right: 16.6%; height: 2px;
    background: repeating-linear-gradient(90deg, var(--border-strong) 0 7px, transparent 7px 14px); z-index: 0;
  }
  @media (max-width: 768px) {
    .workflow-steps { grid-template-columns: 1fr; gap: 44px; }
    .workflow-steps::before { display: none; }
  }
  .workflow-step { display: flex; flex-direction: column; align-items: center; text-align: center; position: relative; z-index: 1; }
  .step-badge {
    width: 46px; height: 46px; border-radius: 50%;
    background: linear-gradient(140deg, #6366f1, #7c3aed); color: #fff;
    font-size: 16px; font-weight: 700; display: flex; align-items: center; justify-content: center;
    margin-bottom: 22px; border: 4px solid var(--bg); box-shadow: 0 8px 20px -6px rgba(109, 74, 232, 0.6);
  }
  .workflow-step h3 { font-size: 16px; font-weight: 600; color: var(--text-primary); margin: 0 0 8px; }
  .workflow-step p { font-size: 13.5px; line-height: 1.55; color: var(--text-body); max-width: 280px; margin: 0; }

  /* ══ AKA ══ */
  .aka { padding: 96px 0; }
  .aka-inner { max-width: 820px; margin: 0 auto; text-align: center; }
  .aka-title {
    font-size: clamp(26px, 3.4vw, 36px); font-weight: 600; letter-spacing: -0.035em;
    color: var(--text-primary); line-height: 1.12; margin: 10px 0 30px;
  }
  .aka-cloud { display: flex; flex-wrap: wrap; justify-content: center; gap: 10px; max-width: 720px; margin: 0 auto 30px; }
  .aka-chip {
    font-family: inherit; cursor: pointer;
    font-size: 14px; font-weight: 500; color: var(--text-body);
    padding: 9px 16px; border-radius: 100px;
    border: 1px solid var(--border-med); background: var(--surface);
    transition: transform 0.2s ease, border-color 0.2s ease, color 0.2s ease, background 0.2s ease, box-shadow 0.2s ease;
  }
  .aka-chip:hover { transform: translateY(-2px); border-color: var(--border-strong); color: var(--text-primary); }
  .aka-chip.is-accent {
    color: #fff; border-color: transparent;
    background: linear-gradient(135deg, #6366f1, #7c3aed);
    box-shadow: 0 6px 16px -8px rgba(99,102,241,0.55);
  }
  .aka-chip.is-accent:hover { color: #fff; transform: translateY(-2px); }
  .aka-foot { font-size: clamp(16px, 2vw, 19px); line-height: 1.5; color: var(--text-body); margin: 0; }
  .aka-foot strong { color: var(--text-primary); font-weight: 600; }

  /* ══ SHOWCASE / COMMERCIAL BAND ══ */
  .showcase { padding: 96px 0; }
  .showcase-inner { display: grid; grid-template-columns: 1.02fr 1fr; gap: 52px; align-items: center; }
  @media (max-width: 860px) { .showcase-inner { grid-template-columns: 1fr; gap: 34px; } }
  .showcase-visual {
    aspect-ratio: 16 / 10; border-radius: 20px; overflow: hidden;
    background: url('/og-image-light.png') left center / cover no-repeat;
    border: 1px solid var(--border-med);
    box-shadow: 0 30px 70px -34px rgba(60,50,140,0.35), var(--shadow-md);
  }
  :global(html[data-theme='dark']) .showcase-visual {
    background-image: url('/og-image.png');
    box-shadow: 0 30px 70px -34px rgba(30,26,90,0.7);
  }
  .showcase-title { text-align: left; }
  .showcase-lead { font-size: 15.5px; line-height: 1.7; color: var(--text-body); margin: 16px 0 22px; }
  .showcase-points { list-style: none; margin: 0 0 28px; padding: 0; display: flex; flex-direction: column; gap: 11px; }
  .showcase-points li { display: flex; align-items: center; gap: 10px; font-size: 14px; color: var(--text-body); }
  .showcase-points svg { width: 16px; height: 16px; color: #34d399; flex: none; }

  /* ══ CTA BANNER ══ */
  .cta-banner {
    position: relative; padding: 116px 24px; text-align: center; overflow: hidden;
    /* Light: pastel aurora + light scrim (dark text). */
    background:
      linear-gradient(180deg, rgba(238,240,248,0.72), rgba(238,240,248,0.82)),
      radial-gradient(ellipse 70% 90% at 50% 50%, rgba(124,58,237,0.12), transparent 70%),
      url('/ornament-light.png') center / cover no-repeat;
    border-top: 1px solid var(--border);
  }
  :global(html[data-theme='dark']) .cta-banner {
    background:
      linear-gradient(180deg, rgba(9,10,15,0.76), rgba(9,10,15,0.84)),
      radial-gradient(ellipse 70% 90% at 50% 50%, rgba(124,58,237,0.30), transparent 70%),
      url('/ornament.png') center / cover no-repeat;
  }
  .cta-banner-inner { max-width: 620px; margin: 0 auto; display: flex; flex-direction: column; align-items: center; }
  .cta-banner-title { font-size: clamp(28px, 5vw, 46px); font-weight: 600; color: var(--text-primary); letter-spacing: -0.04em; margin: 0 0 16px; line-height: 1.08; }
  .cta-banner-desc { font-size: 16px; line-height: 1.6; color: var(--text-body); margin: 0 0 30px; }
  :global(html[data-theme='dark']) .cta-banner-title { color: #fff; }
  :global(html[data-theme='dark']) .cta-banner-desc { color: rgba(237,237,237,0.72); }
  .btn-cta { height: 52px; }
</style>
