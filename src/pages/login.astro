---
// The authenticated experience is the /app SPA (it handles sign-in, account
// creation, unlock-on-reload, and the vault in one place — the account key
// never leaves the browser). Old /login links redirect there.
return Astro.redirect('/app', 302);
---
